import logo from './logo.svg'
import './App.css'

function App() {
  return (
    <rect x={50} y={50} width={100} height={50} color='blue'>
      <circle x={50} y={50} radius={20} color='red' />
    </rect>
  )
}

export default App
